h1.center Список офісних приміщень

= render 'layouts/general/filter_table', path: (can? :new, OfficeArea) ? new_cabinet_office_area_path : nil, title: '' do
  thead
    tr
      th Адреса
      th Статус
      th Активний тип об'єкту
      th Структурний підрозділ
      th Шар на карті
      th Дії
  tbody
    = render 'table'
